comfyui-frontend-package==1.45.15
comfyui-workflow-templates==0.10.0
comfyui-embedded-docs==0.5.4
aigc-shared[media] @ git+https://github.com/xuzihe2020/aigc-shared.git@a1724869fe02f0d43a53a40e582b6e6f7a060750
torch
torchsde
torchvision
torchaudio
numpy>=1.25.0
einops
transformers>=4.50.3
tokenizers>=0.13.3
sentencepiece
safetensors>=0.4.2
aiohttp>=3.11.8
yarl>=1.18.0
pyyaml
Pillow
PyMuPDF
scipy
tqdm
psutil
alembic
SQLAlchemy>=2.0.0
filelock
av>=16.0.0
comfy-kitchen==0.2.10
comfy-aimdo==0.4.10
requests
simpleeval>=1.0.0
blake3

#non essential dependencies:
kornia>=0.7.1
spandrel
pydantic~=2.0
pydantic-settings~=2.0
PyOpenGL
glfw

# --- repo-specific: audio dubbing pipeline (scripts/audio/) ---
# ASR + sentence-level emotion PoC (poc_asr_ser_translate.py).
# torch/torchaudio above already satisfy the funasr/faster-whisper backends.
faster-whisper>=1.0.3
funasr>=1.1.5
soundfile
huggingface_hub
imageio-ffmpeg

# Fish S2 voice-clone PoC (poc_fish_s2_clone.py). Runtime deps of the
# fish_speech engine bundled inside ComfyUI-FishAudioS2 (see --fish-src).
# descript-audio-codec and descript-audiotools must be installed manually
# with --no-deps (their protobuf<5 pin conflicts with this shared venv):
#   .venv/bin/python -m pip install descript-audio-codec --no-deps
#   .venv/bin/python -m pip install "descript-audiotools>=0.7.2" --no-deps
loguru
click
hydra-core>=1.3.2
omegaconf
pyrootutils>=1.0.4
rich>=13.5.3
tiktoken>=0.8.0
librosa>=0.10.1
resampy>=0.4.3
# transitive runtime deps of the descript packages (--no-deps skips them)
flatten-dict
importlib-resources
julius
randomname
ffmpy
argbind
tensorboard

# --- repo-specific: LoRA data face verification (tools/lora_data_generator) ---
# ArcFace identity scoring of generated images vs the primary character
# reference; runs on the onnxruntime pulled in by funasr above. Needs cv2,
# which the ComfyUI/custom-node environment already provides (install
# opencv-python only if `import cv2` fails in a fresh venv).
insightface
